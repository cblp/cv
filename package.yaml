name: cv

ghc-options: -Wall -Werror

library:
  source-dirs: lib

executable:
  dependencies:
    - cv
  main: Main.hs
  other-modules: []

dependencies:
  - base
  - blaze-html
  - bytestring
  - directory
  - filepath
  - process
  - shakespeare
  - temporary
  - text

default-extensions:
  - StrictData
